const OPENED_UNTIL = 19
const CLOSED_AT = [0, 6]

export default class constants{
    static get OPENED_UNTIL(){
        return OPENED_UNTIL
    }

    static get CLOSED_AT(){
        return CLOSED_AT
    }

}